# Nothing here is required to run this prototype locally — the quote checker
# uses no external APIs and no secrets. This file exists so the shape is ready
# for what comes later. Never put real values in this file; copy it to
# .env.local (already gitignored) and fill that in instead.

# --- Not needed yet ---
# SENDGRID_API_KEY=
# AZURE_STORAGE_CONNECTION_STRING=
# STRIPE_SECRET_KEY=
